# Nixpacks 빌드 설정 — Python(FastAPI) 백엔드만 빌드
# Nix 환경에서는 pip 대신 python3 -m pip 를 사용해야 합니다.
providers = ["python"]

[phases.install]
cmds = ["python3 -m pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && python3 -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
