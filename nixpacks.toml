# Nixpacks 빌드 설정 — Python(FastAPI) 백엔드만 빌드
# Flutter(frontend/)는 Railway 배포 대상이 아닙니다.
providers = ["python"]

[phases.install]
cmds = ["pip install -r backend/requirements.txt"]

[start]
cmd = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
